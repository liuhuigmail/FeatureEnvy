* notify batch clusterer listeners * * * * clusterer * * batch clusterer event 
* notify batch clusterer listeners * * * * clusterer * * * bean visual 
* notify batch clusterer listeners * * * * clusterer * * * * instances 
