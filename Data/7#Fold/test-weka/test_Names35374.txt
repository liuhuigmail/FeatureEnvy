* * * set preprocessing * * * kernel filter * * * * filter 
* * * set preprocessing * * * kernel filter * * * * kernel 
* * * set preprocessing * * * kernel filter * * nominal to binary 
* * * set preprocessing * * * kernel filter * * replace missing values 
* * * set preprocessing * * * kernel filter * * * single index 
