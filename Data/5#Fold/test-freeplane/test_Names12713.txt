insert node into without undo * * * node model * * * map controller 
insert node into without undo * * * node model * * * map reader 
insert node into without undo * * * node model * * * map writer 
insert node into without undo * * * node model * * * read manager 
insert node into without undo * * * node model * * * write manager 
